package com.company;

public class Encryption {
    /*
        way to generate key

        way to decrypt database

        way to encrypt database
     */

    /*
        private key

        Public void passInfo(user.name, user.password)
            key = makeMasterKey(user.name, user.password)

        private string makeMasterKey(user.name, user.password)
            return user.name ^ user.password

        public decrytp(data)
            use 
     */
}
